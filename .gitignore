# Java / Maven
target/
*.class
dependency-reduced-pom.xml

# Data & models — fetched via scripts/download-data.sh and
# scripts/download-models.sh, not committed (same convention as the sibling
# `search` project's dataset/ and models/ directories).
data/*.csv
models/*/

# Node / React
web/node_modules/
web/dist/

# Python
training/__pycache__/
*.pyc

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
